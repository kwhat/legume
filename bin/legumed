#!/usr/bin/env php
<?php

$status = 255;
if (PHP_SAPI == "cli") {
    $autoload = __DIR__ . "/../vendor/autoload.php";

    // Load the Composer auto-loader.
    require_once($autoload);

    // Create and start the new service.
    $service = new Legume\Daemon($autoload);
    $status = $service->run();
}

exit($status);
